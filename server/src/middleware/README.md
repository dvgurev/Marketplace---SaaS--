# middleware

Содержит Express middleware функции, применяемые к маршрутам.

## Содержимое

- `auth.ts` — проверка JWT, установка `req.user`.

## Использование

Подключайте нужные middleware в `index.ts` перед роутером или в самом роутере.
