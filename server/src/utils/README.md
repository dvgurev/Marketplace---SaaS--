# utils

Вспомогательные утилиты и обёртки, не зависящие от Express.

## Содержимое

- `hash.ts` — функции для хеширования паролей (`bcrypt`).
- `jwt.ts` — создание и проверка JWT токенов.

## Принцип

Каждая утилита экспортирует чистые функции, которые можно тестировать отдельно.
